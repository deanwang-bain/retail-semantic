import Placeholder from "@/components/placeholder-page";

export default function OntologyPage() {
  return (
    <Placeholder
      title="Ontology Explorer"
      blurb="Force-directed view of the retail knowledge graph — products, concepts, attributes, and the MAPS_TO edges that form the semantic layer."
    />
  );
}
